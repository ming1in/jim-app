import { createMuiTheme } from '@material-ui/core';

export const themeConfig = createMuiTheme({
  palette: {
    background: {
      default: '#F9F3E1'
    },
    primary: {
      main: '#344E41'
    },
    secondary: {
      main: '#A3B18A'
    }
  }
});
